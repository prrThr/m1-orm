{
  "name": "ormm1",
  "version": "1.0.0",
  "description": "* npm init * npm install --save sequelize * npm install --save mysql2 * npm i dotenv",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.0",
    "sequelize": "^6.32.1"
  }
}
